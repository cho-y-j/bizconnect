# 구글 로그인 설정 - 한글 화면 가이드

## 🔍 현재 상태 확인

Google Cloud Console 설정은 완료되었습니다:
- ✅ 승인된 리디렉션 URI에 `https://hdeebyhwoogxawjkwufx.supabase.co/auth/v1/callback` 등록됨
- ✅ 클라이언트 보안 비밀번호 2개 생성됨

## ✅ 해결 방법 (한글 화면 기준)

### Step 1: Google Cloud Console에서 클라이언트 보안 비밀번호 복사

**현재 보고 있는 Google Cloud Console 페이지에서:**

1. **"클라이언트 보안 비밀번호"** 섹션 찾기
2. **"Client Secret 2"** 항목 확인
   - 생성일: 2025년 12월 13일 PM 4시 20분 40초
   - 상태: 사용 설정됨
3. **복사 아이콘** (📋) 클릭
   - 클라이언트 보안 비밀번호가 클립보드에 복사됩니다

**또는:**
- "Client Secret 1" 옆의 복사 아이콘을 사용해도 됩니다

### Step 2: Supabase Dashboard에 입력

1. **Supabase Dashboard 접속**
   - https://supabase.com/dashboard 접속
   - 프로젝트: **bizconnedt** 선택

2. **인증 메뉴로 이동**
   - 왼쪽 사이드바에서 **"인증"** 클릭
   - 상단 탭에서 **"제공업체"** 클릭

3. **Google 설정 열기**
   - 제공업체 목록에서 **"Google"** 클릭

4. **클라이언트 시크릿 입력**
   - **"클라이언트 시크릿(OAuth용)"** 필드 클릭
   - Google Cloud Console에서 복사한 보안 비밀번호 붙여넣기 (Ctrl+V)
   - 앞뒤 공백 확인 (없어야 함)

5. **저장**
   - 오른쪽 하단의 **"구하다"** (저장) 버튼 클릭
   - 저장 완료 메시지 확인

### Step 3: 확인

Supabase Google 설정 페이지에서 다음을 확인:
- [ ] "Google로 로그인 활성화" 토글이 **ON** (초록색)
- [ ] "클라이언트 ID" 필드에 값 입력됨
- [ ] **"클라이언트 시크릿(OAuth용)" 필드에 값 입력됨** ← 가장 중요!
- [ ] "구하다" (저장) 버튼을 눌렀는지 확인

## 🧪 테스트

설정 완료 후:
1. 브라우저 캐시 삭제 (Ctrl+Shift+Delete)
2. https://bizconnect-ten.vercel.app 접속
3. **로그인** 클릭
4. **구글로 로그인** 버튼 클릭
5. Google 계정 선택 및 권한 승인
6. 대시보드로 자동 이동 확인

## ⚠️ 주의사항

- 클라이언트 보안 비밀번호는 **공백 없이 정확히** 입력해야 합니다
- 앞뒤 공백이 있으면 작동하지 않습니다
- 복사할 때 **전체를 복사**했는지 확인하세요
- **"구하다" (저장) 버튼을 반드시 눌러야** 설정이 저장됩니다

## 📝 한글 메뉴 경로 요약

**Google Cloud Console:**
- APIs & Services → 자격 증명 → OAuth 2.0 클라이언트 ID
- 클라이언트 보안 비밀번호 섹션 → 복사 아이콘 클릭

**Supabase Dashboard:**
- 인증 → 제공업체 → Google
- 클라이언트 시크릿(OAuth용) 필드에 붙여넣기
- 구하다 (저장) 버튼 클릭

## 🔄 여전히 안 되면

1. Supabase Dashboard에서 Google 설정 페이지 새로고침
2. 클라이언트 시크릿 필드를 비우고 다시 입력
3. "구하다" (저장) 클릭
4. 1-2분 기다린 후 다시 테스트









