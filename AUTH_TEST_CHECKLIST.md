# 회원가입 및 로그인 테스트 체크리스트

## ✅ 준비 사항

### 1. Supabase 설정 확인
- [ ] Email Provider 활성화 확인
  - Dashboard > Authentication > Providers > Email
  - "Enable Email provider" ON
- [ ] 이메일 확인 비활성화 (개발용)
  - "Confirm email" OFF
- [ ] 트리거 비활성화 (임시)
  - SQL Editor에서 실행: `DROP TRIGGER IF EXISTS on_auth_user_created ON auth.users;`

### 2. 환경 변수 확인
- [ ] `web/.env.local` 파일 존재
- [ ] `NEXT_PUBLIC_SUPABASE_URL` 설정됨
- [ ] `NEXT_PUBLIC_SUPABASE_ANON_KEY` 설정됨

---

## 🧪 테스트 시나리오

### 테스트 1: 회원가입
1. 브라우저에서 `http://localhost:3000` 접속
2. "시작하기" 또는 "회원가입" 클릭
3. 새 이메일 주소 입력 (예: `test@example.com`)
4. 비밀번호 입력 (최소 8자)
5. 비밀번호 확인 입력
6. "회원가입" 버튼 클릭

**예상 결과:**
- ✅ 에러 없이 회원가입 성공
- ✅ 대시보드로 자동 이동 (또는 로그인 페이지로 이동)

**확인 사항:**
- 브라우저 콘솔에 에러 없음
- Supabase Dashboard > Authentication > Users에서 새 사용자 확인
- Table Editor > user_settings에서 레코드 확인

---

### 테스트 2: 로그인
1. 로그인 페이지 접속 (`/auth/login`)
2. 회원가입한 이메일 입력
3. 비밀번호 입력
4. "로그인" 버튼 클릭

**예상 결과:**
- ✅ 에러 없이 로그인 성공
- ✅ 대시보드로 이동
- ✅ 사용자 정보 표시

**확인 사항:**
- 브라우저 콘솔에 에러 없음
- 대시보드 페이지 정상 표시
- 로그아웃 버튼 작동

---

### 테스트 3: 세션 유지
1. 로그인 후 페이지 새로고침
2. 다른 페이지 이동 후 뒤로가기

**예상 결과:**
- ✅ 로그인 상태 유지
- ✅ 대시보드 접근 가능

---

## 🔍 문제 발생 시 확인

### 회원가입 실패
1. 브라우저 콘솔 확인 (F12)
2. 에러 메시지 확인
3. Supabase Dashboard > Logs 확인

### 로그인 실패
1. 이메일/비밀번호 확인
2. Supabase Dashboard > Authentication > Users에서 사용자 상태 확인
3. "Confirm user" 버튼 클릭 (필요 시)

### user_settings 미생성
1. Supabase Dashboard > Table Editor > user_settings 확인
2. 수동으로 생성:
   ```sql
   INSERT INTO user_settings (user_id)
   SELECT id FROM auth.users
   WHERE id NOT IN (SELECT user_id FROM user_settings);
   ```

---

## 📝 테스트 결과 기록

- [ ] 회원가입 성공
- [ ] 로그인 성공
- [ ] user_settings 자동 생성
- [ ] 대시보드 접근 가능
- [ ] 세션 유지 확인

---

**테스트 완료 후 결과를 알려주세요!**

