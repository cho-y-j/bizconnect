# AI 환경 변수 설정 가이드

## ❌ 오류 메시지
```
AI 서비스가 설정되지 않았습니다.
Failed to load resource: the server responded with a status of 500
```

## ✅ 해결 방법

### 1단계: 환경 변수 파일 확인

`web/.env.local` 파일이 있는지 확인하고, 다음 내용을 추가하세요:

```env
# Supabase 설정 (이미 있을 수 있음)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# DeepSeek API 키 (서버 사이드 전용 - 새로 추가)
DEEPSEEK_API_KEY=sk-af010f64eb7d44d0bb82ef6d3ff0d539
```

**중요:**
- `NEXT_PUBLIC_` 접두사 **없이** `DEEPSEEK_API_KEY`로 작성
- 서버 사이드에서만 사용하므로 클라이언트에 노출되지 않음

### 2단계: 파일 생성 방법

**PowerShell에서:**
```powershell
cd web
New-Item -ItemType File -Path .env.local -Force
# 파일을 열어서 위 내용 추가
```

또는 직접 `web/.env.local` 파일을 생성하고 내용 추가

### 3단계: 개발 서버 재시작 (필수!)

환경 변수를 추가/수정한 후에는 **반드시 개발 서버를 재시작**해야 합니다:

```powershell
# 1. 현재 실행 중인 서버 중지 (Ctrl+C)
# 2. 다시 시작
cd web
npm run dev
```

### 4단계: 확인

브라우저에서 다음 URL 접속:
```
http://localhost:3000/api/check-ai-env
```

**예상 결과:**
```json
{
  "hasDeepSeekKey": true,
  "keyPreview": "sk-af010f6...",
  "message": "✅ DeepSeek API 키가 설정되어 있습니다."
}
```

---

## 🔧 임시 해결책

코드에 기본값을 하드코딩했지만, **보안상 권장하지 않습니다**. 

환경 변수로 설정하는 것이 올바른 방법입니다.

---

## 📝 체크리스트

- [ ] `web/.env.local` 파일 존재 확인
- [ ] `DEEPSEEK_API_KEY=sk-af010f64eb7d44d0bb82ef6d3ff0d539` 추가
- [ ] 개발 서버 재시작
- [ ] `/api/check-ai-env` 접속하여 확인
- [ ] AI 추천 기능 테스트

---

## 🐛 여전히 안 되면

1. **파일 위치 확인**
   - `web/.env.local` (프로젝트 루트가 아님!)
   - `web` 폴더 안에 있어야 함

2. **환경 변수 이름 확인**
   - `DEEPSEEK_API_KEY` (대소문자 정확히)
   - `NEXT_PUBLIC_` 접두사 없음

3. **서버 재시작 확인**
   - 환경 변수 변경 후 반드시 재시작
   - 터미널에서 `Ctrl+C` 후 `npm run dev`

4. **브라우저 캐시 클리어**
   - `Ctrl+Shift+R` (강력 새로고침)

