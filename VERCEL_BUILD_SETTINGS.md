# 🔧 Vercel Build and Deployment 설정 가이드

## 📍 설정 위치

현재 보이는 페이지는 **General** 설정입니다.
**Build and Deployment** 설정은 **왼쪽 사이드바**에서 별도로 열어야 합니다.

## 🎯 설정 방법

### Step 1: 왼쪽 사이드바에서 "Build and Deployment" 클릭

현재 페이지에서:
1. 왼쪽 사이드바를 보세요
2. **General** 섹션 아래에 **"Build and Deployment"** 메뉴가 있습니다
3. **"Build and Deployment"** 클릭

### Step 2: Build and Deployment 설정 확인

"Build and Deployment" 페이지에서 다음 설정을 확인하세요:

#### Root Directory
- **값**: `web` ✅
- 이미 설정되어 있다면 그대로 두세요

#### Build Command
- **현재 값 확인**: 무엇으로 설정되어 있는지 확인
- **변경 필요 시**: `npm run build` 또는 **비워두기** (자동 감지)

#### Install Command
- **현재 값 확인**: 무엇으로 설정되어 있는지 확인
- **변경 필요 시**: `npm install` 또는 **비워두기** (자동 감지)
- ⚠️ **`cd web && npm install`이 있다면 제거하세요!**

#### Output Directory
- **값**: `.next` 또는 **비워두기** (자동 감지)

### Step 3: 저장

1. **Save** 버튼 클릭
2. 변경사항이 저장됩니다

### Step 4: 재배포

1. 상단 탭에서 **"Deployments"** 클릭
2. 최신 배포에서 **"..."** 메뉴 클릭
3. **"Redeploy"** 선택
4. **"Use existing Build Cache"** 체크 해제
5. **"Redeploy"** 클릭

---

## 🔍 현재 문제 진단

Root Directory가 `web`으로 설정되어 있다면:

✅ **올바른 설정:**
- Install Command: `npm install` (또는 비워두기)
- Build Command: `npm run build` (또는 비워두기)

❌ **잘못된 설정:**
- Install Command: `cd web && npm install`
- Build Command: `cd web && npm run build`

**Root Directory가 `web`이면, Vercel은 이미 `web` 디렉토리 안에서 작업하므로 `cd web`이 필요 없습니다!**

---

## 📋 체크리스트

- [ ] 왼쪽 사이드바에서 "Build and Deployment" 클릭
- [ ] Root Directory가 `web`인지 확인
- [ ] Install Command에서 `cd web` 제거 (있다면)
- [ ] Build Command에서 `cd web` 제거 (있다면)
- [ ] Save 클릭
- [ ] Deployments에서 재배포 (캐시 없이)

---

**"Build and Deployment" 메뉴를 클릭하면 설정을 볼 수 있습니다!**


